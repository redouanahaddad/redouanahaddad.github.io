---
import { settings } from "../data/settings";
import Container from "./Container.astro";
import SocialList from "./SocialList.astro";
import LittleSticksPlug from "./LittleSticksPlug.astro";
---

<style>
  footer {
    padding: var(--space-m) 0 var(--space-s) 0;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-top: var(--space-s);
    border-top: 1px solid var(--theme-on-background--light);
  }
  .footer-social__list {

  }
  .footer-bottom__container {
    /* padding: var(--space-s) 0; */
    font-size: var(--step--1);
  }
</style>

<Container>
  <footer>
    <!-- You can delete this code or disable it with option in settings.ts -->
    
    <div class="footer-bottom__container ">
      <p>&copy; {new Date().getFullYear()} {settings.name}</p>
    </div>
  </footer>
</Container>